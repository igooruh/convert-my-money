/* const convert = require('./convert')

test('convert 4 to 4', () => {
    expect(convert.convert(4, 4)).toBe(16)
})

test('convert 4 to 4 with ERROR', () => {
    expect(convert.convert(4, 4)).toBe(17)
})

test('toMoney converts Float', () => {
    expect(convert.toMoney(2)).toBe('2.00')
})

test('toMoney converts Float with ERROR', () => {
    expect(convert.toMoney(2)).toBe('2.10')
})

test('toMoney converts String', () => {
    expect(convert.toMoney('2')).toBe('2.00')
}) */